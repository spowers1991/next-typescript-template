/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['placehold.co'], // Add 'a.storyblok.com' to the list of allowed domains
    },
  };
  
  export default nextConfig;
  